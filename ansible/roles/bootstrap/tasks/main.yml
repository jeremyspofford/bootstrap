- name: Install Debian packages (Debian/Ubuntu)
  apt:
    name: "{{ item }}"
    state: present
    update_cache: yes
  loop:
    - gnupg
    - curl
    - unzip
    - stow
    - xclip
    - locate
    - zsh
  when: ansible_os_family == "Debian"
